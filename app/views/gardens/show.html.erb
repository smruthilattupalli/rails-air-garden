<div class="container">
  <div class="row">
    <div class="col">
      <p> <%= link_to "Back to menu", gardens_path, class:"btn btn-primary"  %></p>
      <h1> <%=  @garden.title %> </h1>

      <%= image_tag("", height: 300, width: 400, crop: :fill) %>

      <p><%= @garden.description %></p>

      <p class="bold"><i class="fas fa-hand-holding-usd"></i>  Price</p>
      <p> <%= @garden.price %>€ </p>
      <p class="bold"><i class="fas fa-map-marker-alt"></i>  Location</p>
      <p><%= @garden.location %></p>
      <p class="bold"><i class="fas fa-users"></i>  Capacity</p>
      <p><%= @garden.capacity %></p>
      <p class="bold"><i class="fas fa-ruler-combined"></i>  Size </p>
      <p><%= @garden.size %>m2</p>
      <a class="btn btn-flat" href="#">Book now</a>

    </div>

  </div>
</div>
